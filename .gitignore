.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# training outputs
runs/

# generated self-contained Kaggle submission (regenerate via scripts/package_submission.py;
# embeds weights, ~1.6MB -- don't bloat git)
submission_kaggle.py

# local scratch / investigation dumps
_*.py
*.log
out.txt
err.txt
imperr*.txt
smoke_err.txt

# notebook backups + executed SMOKE copies (junk; regenerate as needed)
*.ipynb.bak*
*.SMOKE.ipynb
*.SMOKE.out.ipynb

# native C++ build artifacts
native/_bld/
native/build/
native/_probe/
orbit_wars_rl/_native/*.pyd
orbit_wars_rl/_native/*.exp
orbit_wars_rl/_native/*.lib
*.obj
*.exe
*.pdb
.env
weights/
# keep REFERENCE_* (authoritative game spec copies) tracked on purpose

# LaTeX build artifacts (keep the .tex source + compiled .pdf tracked)
*.aux
*.fdb_latexmk
*.fls
*.toc
*.out
*.synctex.gz
*.bbl
*.blg
*.lof
*.lot

# notebook training outputs + scratch (regenerate from runs/)
notebooks/metrics.json
notebooks/league_agents/
notebooks/render/
_*.ipynb

# notebook checkpoint dumps (large .pt weights + loss curves; don't bloat git)
notebooks/weight/

# notebook text-export scratch
.nbtmp/
