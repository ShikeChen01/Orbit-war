\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=blue!50!black,urlcolor=blue!50!black]{hyperref}
\usepackage{parskip}
\usepackage{enumitem}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, calc, fit}

\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\KL}{\mathrm{KL}}
\newcommand{\softmax}{\operatorname{softmax}}
\newcommand{\relu}{\operatorname{ReLU}}
\newcommand{\clip}{\operatorname{clip}}
\newcommand{\sg}{\operatorname{stopgrad}}
\newcommand{\Adv}{\hat{A}}
\newcommand{\pol}{\pi_\theta}
\newcommand{\polold}{\pi_{\theta_{\mathrm{old}}}}
\newcommand{\polref}{\pi_{\mathrm{ref}}}

\title{\textbf{Orbit Wars --- RL Formulation and GRPO Training Math}}
\author{Native C++/LibTorch stack}
\date{\today}

\begin{document}
\maketitle
\vspace{-1.5em}
\begin{center}\small This document specifies the policy, the reward, and the
\emph{Group Relative Policy Optimization} (GRPO) update used by the native trainer.
Symbols map to \texttt{native/rl/config.hpp} and \texttt{native/rl/model/}.\end{center}
\tableofcontents
\bigskip

%==============================================================
\section{Problem setup}\label{sec:problem}
Orbit Wars is an $N$-player symmetric Markov game with $N\in\{2,4\}$. The engine sets
$N=\lvert\text{state}\rvert$ (\texttt{num\_agents}) and assigns $N$ home planets from a
4-fold-symmetric quadrant group ($N=2$: two opposite quadrants; $N=4$: all four). Each turn all
players \emph{simultaneously} issue a set of fleet \emph{launches}; the deterministic engine then
moves fleets, resolves collisions/combat, rotates planets, produces ships, and (on scheduled
steps) spawns comets. Scoring is a \textbf{free-for-all}: at termination player $i$'s score is its
total ship count $\mathrm{ships}_i$ (planets $+$ fleets), and the seat(s) with the maximum score
receive reward $+1$, all others $-1$ (so $N=4$ is winner-take-all among four individuals --- not
teams).

\paragraph{Training scope of this document.} We currently train the \textbf{two-player special
case} ($N=2$: ego $p=0$ against one opponent in the opposite quadrant). The native world generator
places exactly two homes; the shaped reward (Section~\ref{sec:reward}) uses the zero-sum margin
$\mathrm{ships}_0-\mathrm{ships}_1$; and each group plays a single opponent (scripted, or a frozen
snapshot, Section~\ref{sec:league}). The policy (Section~\ref{sec:policy}) and the GRPO update
(Section~\ref{sec:grpo}) are \emph{$N$-agnostic}; lifting to 4-player FFA requires only (i) 4-home
world generation, (ii) replacing the zero-sum margin with the FFA return (max-score seat $+1$ else
$-1$, or a rank-based shaping), and (iii) seating $\geq 1$ extra opponents per game. This 2p$\to$4p
gap is an open modeling item, revisited in Section~\ref{sec:reward}.

\paragraph{Factored action space.}
A turn's action is \emph{factored over the ego player's planets} (``entities''). The
observation exposes up to $E$ entity slots ($E=64$). Each owned planet $e$ independently
chooses a class
\begin{equation}
a_e \in \{0,1,\dots,A-1\}, \qquad A = 1 + C\,|\mathcal{F}|,
\end{equation}
where class $0$ is \emph{no-op}, $\mathcal{F}=\{0.25,0.5,0.75,1.0\}$ is the set of
ship-fraction choices, and $C$ is the number of target choices: $C=E$ in
\emph{target mode} (each class $\geq 1$ names a target entity $t$ and a fraction $f$, with
aim angle computed analytically to hit $t$), or $C=$ \texttt{angle\_bins} in
\emph{angle mode} (class names an angle bin and a fraction). The class index for a launch is
\begin{equation}
\text{class}(t,f) = 1 + t\,|\mathcal{F}| + f .
\end{equation}

%==============================================================
\section{Observation encoding}\label{sec:obs}
For ego player $p$ and state $s$, the encoder (\texttt{native/core/encode.hpp}) emits
\begin{itemize}[nosep]
  \item entity features $X\in\R^{E\times F}$ ($F=20$): per planet geometry, ownership
        (ego/ally/enemy/neutral one-hots), ships, production, comet/orbit flags, proximity
        fleet pressure, and \textbf{five incoming-fleet threat features} (below);
  \item entity mask $m\in\{0,1\}^{E}$: $m_e=1$ iff slot $e$ is a real entity;
  \item action mask $u\in\{0,1\}^{E}$: $u_e=1$ iff $e$ is an ego-owned planet with $>0$ ships
        (i.e.\ legally able to launch);
  \item globals $g\in\R^{G}$ ($G=10$): step/phase, board-wide ship and production margins,
        counts, angular velocity, etc.
\end{itemize}

\paragraph{Incoming-fleet threat features (defensive lookahead).}\label{sec:threat}
A purely \emph{reactive} policy cannot defend if it cannot see what is coming: the base
encoder summarized fleets only as a radius-25 proximity sum, which is blind to a fleet's
\emph{heading} (one aimed away is harmless; one aimed in from afar is the danger). We add a
closed-form projection of each in-flight fleet onto the planets. A fleet at position $o$ with
unit heading $\hat d=(\cos\alpha,\sin\alpha)$ and ship count $\nu$ moves at speed
\begin{equation}
v(\nu)=\min\!\Big(v_{\max},\; 1+(v_{\max}-1)\big(\tfrac{\ln \max(1,\nu)}{\ln 1000}\big)^{1.5}\Big),
\qquad v_{\max}=6,
\end{equation}
matching the engine's fleet dynamics. For a planet at $c$ with radius $\varrho$, the ray
$o+t\hat d$ enters the planet disk at
\begin{equation}
t_{\mathrm{ca}} = -\langle o-c,\hat d\rangle,\quad
p^2 = \lVert o-c\rVert^2 - t_{\mathrm{ca}}^2,\quad
t_{\mathrm{hit}} = t_{\mathrm{ca}} - \sqrt{\varrho^2-p^2}\ \ (\text{if } t_{\mathrm{ca}}\!\geq\!0,\ p^2\!\leq\!\varrho^2).
\end{equation}
The fleet is assigned to the planet with the smallest $t_{\mathrm{hit}}\!\geq\!0$, unless the
ray first crosses the sun (same test against the central disk), in which case it is dropped ---
the heuristic ``does this fleet hit anything?'' filter. Its ETA in ticks is
$\tau = t_{\mathrm{hit}}/v(\nu)$. Aggregating per planet $e$ over enemy ($\mathcal E_e$) and
ally ($\mathcal A_e$) inbound fleets, with $I^{\mathrm{en}}_e=\!\sum_{\mathcal E_e}\!\nu$,
$I^{\mathrm{al}}_e=\!\sum_{\mathcal A_e}\!\nu$, and $\tau^{\mathrm{en}}_e,\tau^{\mathrm{al}}_e$
the minimum ETAs, the five features are
\begin{equation}
\Big[\ \widetilde{I^{\mathrm{en}}_e},\ \
e^{-\tau^{\mathrm{en}}_e/\theta},\ \
\widetilde{I^{\mathrm{al}}_e},\ \
e^{-\tau^{\mathrm{al}}_e/\theta},\ \
\tanh\!\big(\Delta_e/\theta_m\big)\ \Big],
\end{equation}
where $\widetilde{x}=\ln(1+x)/\ln 1000$ is the ship-log compressor, $\theta=8$ sets the
imminence half-life, and
$\Delta_e=\mathrm{ships}_e+\mathrm{prod}_e\,\tau^{\mathrm{en}}_e+I^{\mathrm{al}}_e-I^{\mathrm{en}}_e$
is the \emph{hold-margin}: ships on hand, plus production banked until impact, plus
reinforcements, minus the attack ($>0$ holds, $<0$ falls). Imminence and margin are zeroed
when there is no enemy threat. The projection is an approximation (it ignores planet rotation),
but it is computed \emph{identically} in C++ and Python (parity-tested bit-for-bit), so the
policy trained natively behaves identically when served from pure Python.

%==============================================================
\section{Policy network $\pi_\theta$}\label{sec:policy}
The network (\texttt{native/rl/model/policy\_net.hpp}) maps $(X,m,u,g)$ to per-entity class
logits $Z\in\R^{E\times A}$. It has \textbf{no value head}; GRPO needs no critic. Let
$d=128$ (\texttt{hidden}). The trunk is \emph{MLP $+$ residual GLU} (gated linear unit): each
encoder is a two-layer ReLU MLP followed by one residual GLU block, which adds multiplicative
gating and a skip connection on top of the MLP features.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  font=\footnotesize, >={Stealth[round]}, node distance=5mm and 6.5mm,
  io/.style ={draw, rounded corners=1pt, fill=gray!12,  align=center, minimum height=11mm, inner sep=2.5pt},
  enc/.style={draw, rounded corners=3pt, fill=blue!9,   align=center, minimum height=12mm, inner sep=2.5pt},
  glu/.style={draw, rounded corners=3pt, fill=orange!18, align=center, minimum height=12mm, inner sep=2.5pt},
  act/.style={draw, rounded corners=3pt, fill=green!13,  align=center, minimum height=12mm, inner sep=2.5pt}]
\node[io]  (x)              {entities\\$X{:}E{\times}F$};
\node[enc, right=of x]   (ent)  {entity enc.\\MLP\\$F{\to}d{\to}d$};
\node[glu, right=of ent] (eg)   {residual\\GLU};
\node[enc, right=of eg]  (pool) {masked\\mean-pool};
\node[enc, right=of pool](ctx)  {context enc.\\MLP\\$d{+}G{\to}d{\to}d$};
\node[glu, right=of ctx] (cg)   {residual\\GLU};
\node[act, right=of cg]  (ptr)  {pointer\\actor\\$\langle q_{e,f},k_t\rangle$};
\node[io,  right=of ptr] (out)  {logits\\$E{\times}A$};
\draw[->] (x)--(ent); \draw[->] (ent)--(eg); \draw[->] (eg)--(pool);
\draw[->] (pool)--(ctx); \draw[->] (ctx)--(cg); \draw[->] (cg)--(ptr) node[midway,above,font=\scriptsize]{$c$};
\draw[->] (ptr)--(out);
\node[io, below=7mm of ctx] (g) {globals $g$};
\draw[->] (g)--(ctx);
\draw[->, dashed] (eg) to[out=65,in=115]
      node[above, font=\scriptsize] {tokens $h_e$ (keys + query)} (ptr);
\end{tikzpicture}
\caption{Policy network data flow (target mode). Each encoder is a two-layer MLP followed by a
residual GLU block (Fig.~\ref{fig:glu}). The masked mean-pool of the entity tokens $h_e$,
concatenated with the globals $g$, gives the board context $c$; the pointer actor scores every
(launcher $e$, target $t$, fraction $f$) from $h_e$ and $c$ (plus a per-launcher no-op),
producing the $E\times A$ logits, which are then legality-masked.}
\label{fig:arch}
\end{figure}

\paragraph{Residual GLU block.} For an input $z\in\R^{d}$, with three $d\times d$ weight
matrices (gate $W_g$, value $W_v$, output $W_o$),
\begin{equation}
\mathrm{GLU}(z) = \big(W_v z + b_v\big)\odot \sigma\!\big(W_g z + b_g\big),
\qquad
\mathcal{B}(z) = z + W_o\,\mathrm{GLU}(z) + b_o \ \in\R^{d},
\label{eq:glu}
\end{equation}

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  font=\footnotesize, >={Stealth[round]},
  lin/.style={draw, rounded corners=2pt, fill=blue!9, minimum height=7mm, inner sep=3pt},
  op/.style ={draw, circle, fill=green!14, inner sep=0.5pt, minimum size=5mm}]
\node (in) {$z$};
\node[lin, above right=2mm and 10mm of in] (val)  {$W_v z + b_v$};
\node[lin, below right=2mm and 10mm of in] (gate) {$\sigma(W_g z + b_g)$};
\node[op,  right=20mm of in] (mul) {$\odot$};
\node[lin, right=7mm of mul] (outp) {$W_o(\cdot) + b_o$};
\node[op,  right=7mm of outp] (add) {$+$};
\node[right=7mm of add] (y) {$\mathcal{B}(z)$};
\draw[->] (in) -- (val); \draw[->] (in) -- (gate);
\draw[->] (val) -- (mul); \draw[->] (gate) -- (mul);
\draw[->] (mul) -- (outp); \draw[->] (outp) -- (add); \draw[->] (add) -- (y);
\draw[->, dashed] (in) to[out=90, in=90] node[above, font=\scriptsize] {residual} (add);
\end{tikzpicture}
\caption{The residual GLU block $\mathcal{B}$ (Eq.~\ref{eq:glu}): a value branch $W_v z$ and a
sigmoid gate $\sigma(W_g z)$ are multiplied elementwise ($\odot$), projected by $W_o$, and added
back to the input (the residual skip). One such block (its own $d\times d$ weights) follows each
encoder MLP.}
\label{fig:glu}
\end{figure}

where $\sigma$ is the logistic sigmoid and $\odot$ is elementwise. The sigmoid gate lets the
block pass, attenuate, or suppress each feature channel conditionally (e.g.\ amplify the
incoming-threat channels of Section~\ref{sec:threat} when a planet is under attack); the
residual keeps optimization easy. Applied per entity row (encoder 1) or to the pooled
context vector (encoder 2), it shares weights across rows just like the MLP.

\paragraph{Entity encoder (2-layer MLP $+$ GLU).} A shared two-layer MLP over each entity row,
then a residual GLU block ($\mathcal{B}_{\mathrm{ent}}$, its own weights):
\begin{equation}
\tilde h_e = W_2\,\relu\!\big(W_1 x_e + b_1\big) + b_2,
\qquad
h_e = \mathcal{B}_{\mathrm{ent}}(\tilde h_e) \in \R^{d},
\qquad e=1,\dots,E .
\end{equation}

\paragraph{Context encoder (2-layer MLP $+$ GLU).} A mask-aware mean pool, a two-layer MLP on
the pooled tokens concatenated with the globals, then its own residual GLU block
$\mathcal{B}_{\mathrm{ctx}}$:
\begin{equation}
\bar h = \frac{\sum_{e} m_e\, h_e}{\max\!\big(\sum_e m_e,\,1\big)},
\qquad
\tilde c = W_4\,\relu\!\big(W_3\,[\,\bar h\,;\,g\,] + b_3\big) + b_4,
\qquad
c = \mathcal{B}_{\mathrm{ctx}}(\tilde c) \in\R^{d}.
\end{equation}

\paragraph{Actor head.} Two interchangeable heads produce the raw logits $\tilde Z$:
\begin{description}[nosep,leftmargin=1.2em]
\item[Angle mode:] an MLP on each entity token concatenated with the context,
\begin{equation}
\tilde z_e = W_6\,\relu\!\big(W_5\,[\,h_e\,;\,c\,] + b_5\big) + b_6 \in\R^{A}.
\end{equation}
\item[Target mode (pointer actor):] for each launcher $e$ and fraction $f$ a query
$q_{e,f}\in\R^{d}$ (from $W_q[\,h_e;c\,]$ reshaped to $|\mathcal{F}|\times d$), for each target
$t$ a key $k_t=W_k h_t$, and a per-launcher no-op logit $n_e=w_n^{\!\top}[\,h_e;c\,]$:
\begin{equation}
\mathrm{score}(e,t,f) = \frac{\langle q_{e,f},\,k_t\rangle}{\sqrt{d}},
\qquad
\tilde z_{e,0}=n_e,\quad \tilde z_{e,\,\text{class}(t,f)} = \mathrm{score}(e,t,f).
\end{equation}
The score depends on \emph{both} launcher and target encodings, so the head can rank targets
by geometry (the property a plain MLP actor lacks).
\end{description}

\paragraph{Legality masking.} Illegal classes are set to $-\infty$ (numerically $-10^{9}$).
Class $0$ (no-op) is always legal. For a launch class $(t,f)$,
\begin{equation}
Z_{e,\,\text{class}(t,f)} =
\begin{cases}
\tilde z_{e,\,\text{class}(t,f)} & \text{if } u_e=1 \ \wedge\ m_t=1 \ \wedge\ t\neq e \quad(\text{target mode}),\\[2pt]
\tilde z_{e,\,\text{class}(t,f)} & \text{if } u_e=1 \quad(\text{angle mode}),\\[2pt]
-\infty & \text{otherwise.}
\end{cases}
\end{equation}

\paragraph{Layer inventory (target mode, $d=128$).} All layers are \texttt{nn.Linear}; the
trunk is the two MLPs each followed by one residual GLU block, then the pointer actor.
\begin{center}\small
\begin{tabular}{@{}lll@{}}
\toprule
Module & Layers (name: shape) & Count \\
\midrule
Entity encoder & \texttt{ent0}:$F{\to}d$, \texttt{ent2}:$d{\to}d$;\ GLU \texttt{ent\_glu\_\{gate,val,out\}}:$d{\to}d$ & 5 \\
Context encoder & \texttt{ctx0}:$d{+}G{\to}d$, \texttt{ctx2}:$d{\to}d$;\ GLU \texttt{ctx\_glu\_\{gate,val,out\}}:$d{\to}d$ & 5 \\
Pointer actor & \texttt{aq}:$2d{\to}|\mathcal F|d$,\ \texttt{ak}:$d{\to}d$,\ \texttt{anoop}:$2d{\to}1$ & 3 \\
\midrule
\multicolumn{2}{@{}l}{\emph{Trainable total (no critic --- GRPO)}} & \textbf{13} \\
\bottomrule
\end{tabular}
\end{center}
\noindent (The Python serving net additionally carries a 2-layer critic
\texttt{critic.0/.2} for legacy PPO tooling; the native GRPO trainer and arena never
instantiate or load it.) With $F{=}20$, $G{=}10$, $d{=}128$, $|\mathcal F|{=}4$ this is
$\approx 0.18$M parameters.

%==============================================================
\section{Action distribution}\label{sec:dist}
Each entity is an independent masked categorical; a turn's joint action is their product over
\emph{real} entities (\texttt{native/rl/model/distribution.hpp}):
\begin{align}
\pol(a_e\mid s) &= \softmax(Z_e)_{a_e}, &
\pol(a\mid s) &= \prod_{e:\,m_e=1}\pol(a_e\mid s).
\end{align}
Hence the joint log-probability and entropy are entity sums:
\begin{align}
\log\pol(a\mid s) &= \sum_{e} m_e\,\log\softmax(Z_e)_{a_e},
\label{eq:logp}\\
\mathcal{H}\big[\pol(\cdot\mid s)\big] &= \sum_{e} m_e\,
\Big(-\!\sum_{a'} \softmax(Z_e)_{a'}\,\log\softmax(Z_e)_{a'}\Big).
\label{eq:ent}
\end{align}
Sampling draws $a_e\sim\mathrm{Categorical}(\softmax(Z_e))$ per entity (greedy uses
$\arg\max_a Z_{e,a}$ at evaluation).

%==============================================================
\section{Reward and return}\label{sec:reward}
\paragraph{Production-only dense reward.} The shaped reward rewards \emph{growing one's own
production efficiently}, and nothing else --- not raw planet count, not ship count (both of
which an agent can maximize while still losing the game). Let
$\Pi_0(s)=\sum_{p:\,\mathrm{owner}(p)=0}\mathrm{prod}(p)$ be ego's total planetary production,
read directly from the state. For the ego launches $\mathcal L_t$ issued at step $t$, classify
each by the closed-form projection of Section~\ref{sec:threat}: it \emph{hits} if its fleet
reaches some planet, else it is \emph{wasted}. With $h_t,m_t$ the hit/wasted counts, weights
$w_{\Pi}$ (\texttt{prod\_reward\_weight}), $\rho^{+}$ (\texttt{launch\_hit\_reward}),
$\rho^{-}$ (\texttt{launch\_miss\_penalty}), and scale/clip $\sigma,\kappa$,
\begin{equation}
r_t = \clip\!\left(\frac{\,w_{\Pi}\big(\Pi_0(s_{t+1})-\Pi_0(s_t)\big)
   + \rho^{+} h_t - \rho^{-} m_t\,}{\sigma},\,-\kappa,\,\kappa\right).
\end{equation}
Capturing a high-production planet is rewarded \emph{exactly} through $\Delta\Pi_0$; a fleet
launched to land nowhere is penalized the instant it is fired; a productive launch earns a
small bonus (which also pulls the policy off the passive ``never launch'' optimum).

\paragraph{Outcome and the loss forfeit.} At termination the game is scored by total ships;
$o_i=+1$ win, $-1$ loss, $0$ draw. Let $D_i=\sum_t\gamma^t r_{i,t}$ be the accumulated discounted
dense reward. It is kept on a win but \textbf{forfeited on a loss} --- the return negates it, so
a trajectory that hoarded production yet still lost scores a large \emph{negative}
($\texttt{loss\_forfeit}$; $w_{\mathrm d}=$\texttt{dense\_weight}, $w_{\mathrm o}=$\texttt{outcome\_weight}):
\begin{equation}
\boxed{\;
R_i = \begin{cases}
\ \ w_{\mathrm d} D_i + w_{\mathrm o}, & o_i>0\ \text{(win)},\\[2pt]
\ \ w_{\mathrm d} D_i, & o_i=0\ \text{(draw)},\\[2pt]
-\,w_{\mathrm d} D_i - w_{\mathrm o}, & o_i<0\ \text{(loss)}.
\end{cases}
\;}
\label{eq:return}
\end{equation}
Winning thus dominates by at least $2w_{\mathrm o}$, and among losses the \emph{more} a
trajectory accumulated without converting it to a win, the worse it scores --- directly
penalizing ``maximize my own production/ships but lose the game,'' which earlier shaped rewards
failed to discourage. Setting \texttt{loss\_forfeit}=false recovers the plain
$R_i=w_{\mathrm d}D_i+w_{\mathrm o}o_i$.

\paragraph{Note --- 2p vs.\ 4p objective.} The margin potential $\Phi$ and the outcome $o_i$ are
the two-player ($N=2$) zero-sum forms. In $N=4$ free-for-all the objective is \emph{ordinal} ---
be the maximum of four --- which is not zero-sum: e.g.\ $\Phi$ would become
$\mathrm{ships}_0 - \max_{k\neq 0}\mathrm{ships}_k$ (margin over the \emph{strongest} rival, or a
soft-max thereof) and $o_i$ the FFA win/lose of Section~\ref{sec:problem}. The GRPO machinery
(advantage, surrogate, KL) is unchanged --- only $R_i$ in Eq.~\eqref{eq:return} is redefined.

%==============================================================
\section{Curriculum}\label{sec:curriculum}
From-scratch and BC-finetuned RL repeatedly plateaued at $\sim$random level \emph{versus}
the scripted \texttt{starter} ($\le 2\%$ win), because two skills are entangled in the full
adversarial task: (i) \emph{aim + expand} --- launch ships that actually capture planets ---
and (ii) \emph{defend} --- hold planets against incoming fleets. We decouple them with a
three-stage curriculum (\texttt{RolloutConfig::stage}); each stage warm-starts from the
previous stage's policy, which also becomes the new KL reference $\polref$ of
Section~\ref{sec:kl} (anchor-and-advance, rather than anchoring forever to the passive BC).
\emph{All stages share the one reward of Section~\ref{sec:reward}} (production-only dense $+$
launch quality, loss-forfeit return); only the \textbf{opponent} and the discount $\gamma$
change across stages.

\paragraph{Stage 1 --- solo expansion (passive opponent).} The opponent issues no launches, so
the policy can freely learn to aim and to take the whole map without adversarial punishment.
Because the passive opponent hoards production on its home planet, the ego must actually
\emph{conquer} it (not merely grab neutrals) to win the terminal ship score --- failing that,
the loss forfeit (Eq.~\eqref{eq:return}) turns the episode negative. Running with $\gamma<1$
($\gamma=0.99$) makes \emph{earlier} production gains worth more, i.e.\ a discounted
``planet-takeover-time'' incentive for fastest full-map conquest; the episode ends early once
the ego owns everything.

\paragraph{Stage 2 --- $1$v$1$ vs.\ starter.} The opponent is always \texttt{starter} and
$\gamma=1$ (timing is now dictated by the opponent, not a solo clock). The same reward applies,
so the loss forfeit is fully in force: the agent must convert its production into an actual win
and must \emph{defend} (using the threat features of Section~\ref{sec:threat}) or forfeit
everything it built. The Stage-1 policy supplies both the initialization and $\polref$, so it
keeps its aiming/expansion skill while learning to win.

\paragraph{Stage 3 --- mixed opponents.} The opponent per group is sampled from
\{\texttt{random}, \texttt{starter}, (league snapshots)\} as in Section~\ref{sec:league},
generalizing beyond a single fixed adversary toward the multi-opponent FFA setting.

%==============================================================
\section{GRPO update}\label{sec:grpo}
GRPO replaces the value baseline of actor--critic methods with a \emph{group-relative}
baseline: the policy plays a group of trajectories from the \emph{same} start against the
\emph{same} opponent, and each trajectory's return is centered against its group.

\subsection{Grouped sampling}
For iteration we draw $N$ groups (\texttt{num\_groups}); group $j$ fixes an initial state
$s_0^{(j)}$ (a world) and an opponent, and rolls out $G$ trajectories (\texttt{group\_size})
under the current behavior policy $\polold$ (stochastic). The only within-group variation is
the policy's own action sampling, so the group mean is a valid, low-variance baseline. The
env count is $N\cdot G$ (\texttt{num\_envs}). For every transition we store
$(X,m,u,g)$, the action $a_{i,t}$, the behavior log-prob $\log\polold(a_{i,t}\mid s_{i,t})$,
and the reference log-prob $\log\polref(a_{i,t}\mid s_{i,t})$ (Section~\ref{sec:kl}).

\subsection{Group-relative advantage (no critic)}
Let $\mathcal{G}_j$ be the trajectories of group $j$, with mean and standard deviation
\begin{equation}
\mu_j = \frac{1}{G}\sum_{i\in\mathcal{G}_j} R_i,
\qquad
\varsigma_j = \sqrt{\frac{1}{G}\sum_{i\in\mathcal{G}_j}(R_i-\mu_j)^2}.
\end{equation}
The advantage of trajectory $i$ (group $j=j(i)$), \emph{broadcast to all of its timesteps}, is
\begin{equation}
\boxed{\;
\Adv_i =
\begin{cases}
\dfrac{R_i-\mu_j}{\varsigma_j+\varepsilon}, & \text{whiten\_advantage = true},\\[1.2em]
R_i-\mu_j, & \text{otherwise,}
\end{cases}
\;}
\label{eq:adv}
\end{equation}
with floor $\varepsilon=$\texttt{adv\_eps}. There is no learned $V(s)$ and no temporal-difference
or GAE term --- the entire baseline is Eq.~\eqref{eq:adv}.

\subsection{Clipped policy surrogate}
With the per-step importance ratio
\begin{equation}
\rho_{i,t}(\theta) = \exp\!\Big(\log\pol(a_{i,t}\mid s_{i,t}) - \log\polold(a_{i,t}\mid s_{i,t})\Big),
\end{equation}
(each log-prob the entity sum of Eq.~\eqref{eq:logp}), the surrogate is the PPO clip objective
\begin{equation}
\mathcal{L}^{\mathrm{clip}}(\theta) =
-\,\frac{1}{\sum_i T_i}\sum_{i,t}
\min\!\Big(\rho_{i,t}\,\Adv_i,\ \ \clip(\rho_{i,t},\,1-\epsilon,\,1+\epsilon)\,\Adv_i\Big),
\label{eq:clip}
\end{equation}
with clip range $\epsilon=$\texttt{clip}.

\subsection{KL anchor to a reference policy}\label{sec:kl}
A reference policy $\polref$ (the fixed behavior-cloning checkpoint) anchors training, which
counteracts the empirically observed drift of plain PPO away from the strong BC behavior. We
penalize the unbiased, non-negative $k_3$ estimator of $\KL(\pol\,\|\,\polref)$. With
$\Delta_{i,t}=\log\polref(a_{i,t}\mid s_{i,t})-\log\pol(a_{i,t}\mid s_{i,t})$,
\begin{equation}
\widehat{\KL}_{i,t} = \exp(\Delta_{i,t}) - \Delta_{i,t} - 1 \ \ (\geq 0),
\qquad
\mathcal{L}^{\mathrm{KL}}(\theta) = \frac{1}{\sum_i T_i}\sum_{i,t}\widehat{\KL}_{i,t}.
\label{eq:kl}
\end{equation}

\subsection{Total objective}
Combining the surrogate, the KL anchor (weight $\beta=$\texttt{kl\_beta}) and an entropy bonus
(weight $c_{\mathcal H}=$\texttt{ent\_coef}, from Eq.~\eqref{eq:ent}):
\begin{equation}
\boxed{\;
\mathcal{L}(\theta) =
\mathcal{L}^{\mathrm{clip}}(\theta)
\;+\; \beta\,\mathcal{L}^{\mathrm{KL}}(\theta)
\;-\; c_{\mathcal H}\,\overline{\mathcal{H}}\big[\pol\big]
\;}
\label{eq:loss}
\end{equation}
where $\overline{\mathcal H}$ is the mean entropy over transitions.

\subsection{Optimization}
For each iteration: collect the grouped trajectories under $\polold=\pol$; compute returns
\eqref{eq:return} and advantages \eqref{eq:adv}; then for \texttt{update\_epochs} passes over
\texttt{minibatches} shuffled minibatches, take Adam steps on \eqref{eq:loss} with learning
rate $\eta=$\texttt{lr}, $\epsilon_{\mathrm{Adam}}=$\texttt{adam\_eps}, and global gradient-norm
clip \texttt{max\_grad\_norm}. Advantages are computed once per iteration (off $\polold$); the
ratio \eqref{eq:clip} and KL \eqref{eq:kl} are recomputed each minibatch under the live $\pol$.

%==============================================================
\section{Self-play league}\label{sec:league}
Once training has reached \texttt{start\_step}, the opponent for each group is sampled from a
mix of \{\,\texttt{random}, \texttt{starter}, league\,\} with weights
$(\,$\texttt{mix\_random}, \texttt{mix\_starter}, \texttt{mix\_pool}$\,)$. The league is a
bounded set (\texttt{pool\_capacity}) of \emph{historical} frozen snapshots --- not a single
latest self --- which avoids the cycling/forgetting of single-snapshot self-play. Promotion is
\emph{win-gated}: a new snapshot is admitted only when the current policy beats the pool with
win rate $\geq$ \texttt{promote\_winrate}, so the policy never trains against a regression of
itself.

%==============================================================
\section{Symbols $\leftrightarrow$ \texttt{config.hpp}}
\begin{center}\small
\begin{tabular}{@{}lll@{}}
\toprule
Symbol & \texttt{config.hpp} field & Default \\
\midrule
$d$ & \texttt{ModelConfig::hidden} & $128$ \\
$E$ & \texttt{ModelConfig::max\_entities} & $64$ \\
$\mathcal{F}$ & \texttt{ModelConfig::fractions} & $\{.25,.5,.75,1\}$ \\
$G,\,N$ & \texttt{GrpoConfig::group\_size}, \texttt{num\_groups} & $8,\ 64$ \\
$\epsilon$ & \texttt{GrpoConfig::clip} & $0.2$ \\
$\beta$ & \texttt{GrpoConfig::kl\_beta} & $0.04$ \\
$c_{\mathcal H}$ & \texttt{GrpoConfig::ent\_coef} & $0.01$ \\
$\varepsilon$ & \texttt{GrpoConfig::adv\_eps} & $10^{-4}$ \\
$w_{\mathrm d},\,w_{\mathrm o}$ & \texttt{GrpoConfig::dense\_weight}, \texttt{outcome\_weight} & $1,\ 1$ \\
$\gamma$ & \texttt{GrpoConfig::gamma} & $1.0$ \\
$w_{\Pi}$ & \texttt{RolloutConfig::prod\_reward\_weight} & $10$ \\
$\rho^{+}$ & \texttt{RolloutConfig::launch\_hit\_reward} & $0.5$ \\
$\rho^{-}$ & \texttt{RolloutConfig::launch\_miss\_penalty} & $1.0$ \\
\texttt{loss\_forfeit} & \texttt{RolloutConfig::loss\_forfeit} & true \\
$\sigma,\,\kappa$ & \texttt{RolloutConfig::reward\_scale}, \texttt{reward\_clip} & $50,\ 5$ \\
stage & \texttt{RolloutConfig::stage} & $2$ \\
$\theta,\,\theta_m$ & threat $\tau$-scale, margin-scale (\texttt{encode.hpp}) & $8,\ 100$ \\
$\eta$ & \texttt{OptimConfig::lr} & $3\!\times\!10^{-4}$ \\
\bottomrule
\end{tabular}
\end{center}

\appendix
%==============================================================
\section{Appendix: the PPO/GAE method being replaced}
The previous trainer (\texttt{native/rl/trainer.hpp}) used actor--critic PPO with a value head
$V_\phi$ and Generalized Advantage Estimation. For completeness, with TD residual
$\delta_t = r_t + \gamma V_\phi(s_{t+1}) - V_\phi(s_t)$,
\begin{align}
\Adv^{\mathrm{GAE}}_t &= \sum_{l\geq 0}(\gamma\lambda)^{l}\,\delta_{t+l},
&
\mathcal{L}^{V} &= \tfrac12\,\big(V_\phi(s_t) - \hat R_t\big)^2,
\quad \hat R_t = \Adv^{\mathrm{GAE}}_t + V_\phi(s_t),
\end{align}
and total loss $\mathcal{L}^{\mathrm{clip}} + c_V\,\mathcal{L}^{V} - c_{\mathcal H}\overline{\mathcal H}$.
GRPO removes $V_\phi$ (and thus $\lambda$, $c_V$, and value warmup) entirely, replacing
$\Adv^{\mathrm{GAE}}_t$ with the group-relative $\Adv_i$ of Eq.~\eqref{eq:adv}.

\end{document}
